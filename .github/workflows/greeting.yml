on: [pull_request, issues]

jobs:
   greeting:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/first-interaction@v1
       with:
        repo-token: ${{ secrets.TOKEN }}
        issues-message: 'Thanks for taking the time to create issue 🥰'
        pr-message: 'Congratulations, just made your first contribution to the project :)'